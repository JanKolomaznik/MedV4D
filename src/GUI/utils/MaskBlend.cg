struct Output {
	float4 color : COLOR;
};

Output MaskBlend( float2 texCoord : TEXCOORD0, 
			uniform sampler2D texture : TEX0,
			uniform float4 blendColor : COLOR
			)
{
	Output OUT;
	OUT.color = blendColor;
	OUT.color.w = OUT.color.w*tex2D(texture,texCoord).x;
	return OUT;
}
