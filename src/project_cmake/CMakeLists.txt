CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(MEDV4D)

SET(LIBRARY_OUTPUT_PATH ${CMAKE_SOURCE_DIR}/../lib)
SET(EXECUTABLE_OUTPUT_PATH ${CMAKE_SOURCE_DIR}/../executables)

#SET(BOOST_INCLUDEDIR ${CMAKE_SOURCE_DIR}/../include)
#SET(BOOST_LIBRARYDIR ${CMAKE_SOURCE_DIR}/../lib)

#SET(DCMTK_DIR /usr/local/dicom)
#INCLUDE_DIRECTORIES(${DCMTK_DIR}/include ${CMAKE_SOURCE_DIR}/../ ${CMAKE_SOURCE_DIR}/../include /usr/local/include)
#LINK_DIRECTORIES(${DCMTK_DIR}/lib ${CMAKE_SOURCE_DIR}/../lib /usr/local/lib)

SET(QT_USE_QTOPENGL 1)

SET(CMAKE_MODULE_PATH ".") 

FIND_PACKAGE(Boost REQUIRED COMPONENTS filesystem system thread)
FIND_PACKAGE(OpenGL REQUIRED)
FIND_PACKAGE(VTK REQUIRED)
FIND_PACKAGE(Qt4 REQUIRED)
FIND_PACKAGE(DCMTK REQUIRED)

IF(WIN32)
  SET(CMAKE_CXX_FLAGS "-DDEBUG_LEVEL=10 -DDEBUG_ADITIONAL_INFO")
ELSE(WIN32)
  SET(CMAKE_CXX_FLAGS "-ggdb -Wall -Wno-deprecated -DDCOMPAT_H -DHAVE_CONFIG_H -DHAVE_SSTREAM -DDEBUG_LEVEL=10 -DDEBUG_ADITIONAL_INFO")
ENDIF(WIN32)

ADD_SUBDIRECTORY(common)
ADD_SUBDIRECTORY(backendForDICOM)
ADD_SUBDIRECTORY(Imaging)
ADD_SUBDIRECTORY(m4dWidgets)
ADD_SUBDIRECTORY(vtkIntegration)
#ADD_SUBDIRECTORY(cellCommon)
#ADD_SUBDIRECTORY(cellClient)
#ADD_SUBDIRECTORY(cellServer)

ADD_SUBDIRECTORY(m4dPilot)
#ADD_SUBDIRECTORY(BoneSegmentation)
#ADD_SUBDIRECTORY(SimpleMIP)
